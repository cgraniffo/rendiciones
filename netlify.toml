[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Runtime oficial de Next.js en Netlify (SSR, server actions, middleware/proxy).
# Netlify lo instala automáticamente al detectar Next; lo dejamos explícito.
[[plugins]]
  package = "@netlify/plugin-nextjs"
